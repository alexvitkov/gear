

for := form((initial, condition, increment, body), {
    block := '{};

    loop := '(while(false){});
    loop.condition = condition;

    (loop.body.push)(body); 
    (loop.body.push)(increment); 

    (block.push)(initial);
    (block.push)(loop);

    block;
});