// implement a c-like for loop backed by a while loop
for := form((initial, condition, increment, body), {
    eval ('{
        #initial;
        while (#condition) {
              #body;
              #increment;
        }
    });
});

for(i := 0, i<5, i=i+1, {
    print("i is ", i)
})